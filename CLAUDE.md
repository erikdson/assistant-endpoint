## IMPORTANT:
* We fully leverage the Vercel AI SDK in this project. All backend work should follow best practices from official Vercel AI SDK documentation
* For our Chat UI, we mainly rely on the AI Elements component library that should work well with the AI SDK. For tool calling outputs, we can design our own components - but otherwise we should stick to waht is the standard at all times.
* Be very careful to re-invent things we might already have built in the project. Make sure to clean up unused code and keep the project clean.